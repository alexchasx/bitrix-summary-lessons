
Ядро D7 -> ORM -> Выборка данных:

- Какие параметры принимает метод ::getList и что возращает?
- Зачем нужен ->fetch()?
- Зачем нужен ->fetchAll()?
- Как использовать алиасы?
- Как выбрать все поля и выбирутся ли при это поля-выражения ExpressionField?
- Какой формат использования параметра 'filter' (к примеру как задать WHERE ID=1)?
- Как задавать условия с AND/OR?
- Какие операторы сравнения можно использовать в фильтре?
- Какой оп-р выполнится, если не указывать явно оператор сравнения '='
- Пример ::getList со параметрами.
- Обязательно ли использовать 'runtime', если вычисляемое поле необходимо только в секции `select` (если нет, то как)?
- Можно ли в 'runtime' использовать не только Expression поля, но и поля других типов (и будут ли эти поля доступы в следующих вызовах getList)? 
- Как использовать кеширование выборки?
- Как закешировать выборки с JOIN?
- Когда происходит сброс кеша?
- Как сделать принудительный сброс кеша для таблицы?
- Какой метод производит выборку только по ID?
- Какой метод производит выборку по первичному ключу с возможностью указания дополнительных параметров?
- Что производит ::getRowById и что он возращает?
- Что производит ::getRow и что он возращает?
- Для чего нужен объект Entity\Query?
- Пример использования Entity\Query.
- Можно ли с помощью Entity\Query построить запрос, но не выполнить его?
- Список методов Entity\Query.
- 

Интеграция дизайна -> использование прав доступа

- 
- 
- 
- 
- 