
Док.разработчика
 Основные сведения
  Спец.константы
  Спец.переменные

clear_cache = Y  (отключает кеширование на текущей странице)

ShowTitle() - заголовок страницы и окна браузера (задается через SetPageProperty())
ShowTitle(false) - заголовок страницы (приоритет) (задается через SetTitle())